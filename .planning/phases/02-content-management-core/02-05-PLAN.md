---
phase: 02-content-management-core
plan: "05"
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/router/index.tsx
  - frontend/src/pages/Admin/AdminLayout.tsx
  - frontend/src/pages/Admin/AdminOverview.tsx
  - frontend/src/pages/Admin/AdminArticles.tsx
  - frontend/src/pages/Admin/AdminNotes.tsx
  - frontend/src/pages/Admin/AdminProjects.tsx
  - frontend/src/pages/Admin/AdminFriendLinks.tsx
  - frontend/src/pages/Admin/AdminGuestbook.tsx
  - frontend/src/pages/Admin/AdminUsers.tsx
  - frontend/src/components/admin/AdminSidebar.tsx
  - frontend/src/components/admin/AdminTopbar.tsx
  - frontend/src/components/admin/AdminRouteGuard.tsx
  - frontend/src/components/layout/Sidebar.tsx
autonomous: true
requirements:
  - ADMN-01
must_haves:
  truths:
    - "Only admin users can enter the `/admin` route tree."
    - "Admin users see the admin entry in the public blog sidebar."
    - "The admin workspace has a sidebar, top bar, overview, and route-complete pages for all Phase 2 admin sections."
    - "The visual shell follows the approved pale teal, white-card, dense-admin layout contract."
    - "Decision coverage: D-08: D-09: D-10: D-11: D-12: D-26: D-29: D-30:"
  artifacts:
    - path: "frontend/src/components/admin/AdminRouteGuard.tsx"
      provides: "Admin-only frontend route gate"
      contains: "role"
    - path: "frontend/src/pages/Admin/AdminLayout.tsx"
      provides: "Independent admin layout"
      contains: "AdminSidebar"
    - path: "frontend/src/components/layout/Sidebar.tsx"
      provides: "Conditional admin entry in public sidebar"
      contains: "/admin"
  key_links:
    - from: "frontend/src/router/index.tsx"
      to: "frontend/src/components/admin/AdminRouteGuard.tsx"
      via: "admin route element wraps AdminLayout"
      pattern: "AdminRouteGuard"
    - from: "frontend/src/components/layout/Sidebar.tsx"
      to: "frontend/src/stores/authStore.ts"
      via: "role-based conditional nav item"
      pattern: "role.*admin"
---

<objective>
Create the admin route shell and visual frame.

Purpose: Phase 2 needs a protected `/admin` workspace before content and management pages can be filled in.
Output: Protected admin route tree, admin layout, sidebar, top bar, overview, route-complete page files, and conditional public-sidebar entry.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-content-management-core/02-CONTEXT.md
@.planning/phases/02-content-management-core/02-UI-SPEC.md
@frontend/src/router/index.tsx
@frontend/src/pages/Layout.tsx
@frontend/src/components/layout/Sidebar.tsx
@frontend/src/components/layout/Header.tsx
@frontend/src/stores/authStore.ts
@frontend/src/hooks/useCurrentIdentity.ts
@frontend/src/components/ui/card.tsx
@frontend/src/components/ui/table.tsx
</context>

<source_audit>
GOAL: provides the management backstage where the blogger creates and manages content.
REQ: covers ADMN-01.
RESEARCH: follows frontend admin route hierarchy and existing `apiClient`/auth store boundaries.
CONTEXT: implements D-08, D-09, D-10, D-11, D-12, D-26, D-29, and D-30.
UI-SPEC: implements admin shell, sidebar, top bar, overview, responsive, color, typography, and copy contracts.
</source_audit>

<decision_coverage>
D-08: `/admin` is planned as a real content production workspace with independent layout and states.
D-09: public sidebar admin entry is visible only to admin/blogger.
D-10: route shell prepares table-first content management pages.
D-11: route shell prepares full-page create/edit screens.
D-12: route shell prepares single-item actions and filters for content lists.
D-26: admin route guard enforces admin-only UI access.
D-29: admin sidebar follows content-first ordering.
D-30: overview contains content counts, drafts, recent edits, and quick-create actions without analytics.
</decision_coverage>

<threat_model>
STRIDE register:
- T05-01 Elevation of privilege: visitor opens `/admin` directly. Mitigation: route guard checks authenticated admin role for UX and backend plans enforce `@Roles('admin')` for writes per D-26.
- T05-02 Information disclosure: admin entry visible to visitors. Mitigation: public sidebar renders admin nav item only when `user.role === 'admin'` per D-09.
- T05-03 Spoofing: stale persisted auth state could appear admin after token expiry. Mitigation: guard combines persisted state with current identity refresh flow and redirects to `/owner-login?returnTo=/admin...`.
</threat_model>

<tasks>
<task type="auto">
  <name>Task 1: Add protected admin route tree</name>
  <files>frontend/src/router/index.tsx, frontend/src/components/admin/AdminRouteGuard.tsx, frontend/src/pages/Admin/AdminLayout.tsx</files>
  <read_first>frontend/src/router/index.tsx, frontend/src/api/client.ts, frontend/src/stores/authStore.ts, frontend/src/pages/Auth/OwnerLogin.tsx</read_first>
  <action>Add `/admin` as an independent route tree with `AdminRouteGuard` wrapping `AdminLayout`. The guard must allow only authenticated users with `role === 'admin'`, and otherwise redirect to `/owner-login?returnTo=` with the original admin URL. Keep the route tree separate from the public `Layout` per D-08 and UI-SPEC. Avoid adding public article/note/project frontend pages in this phase.</action>
  <verify>
    <automated>cd frontend; npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - Router contains a `/admin` route tree.
    - Admin route is not nested under public `Layout`.
    - Guard checks admin role and preserves returnTo.
  </acceptance_criteria>
  <done>Admin routes are protected and routed independently.</done>
</task>

<task type="auto">
  <name>Task 2: Build admin layout, sidebar, top bar, overview, and route pages</name>
  <files>frontend/src/pages/Admin/AdminLayout.tsx, frontend/src/pages/Admin/AdminOverview.tsx, frontend/src/pages/Admin/AdminArticles.tsx, frontend/src/pages/Admin/AdminNotes.tsx, frontend/src/pages/Admin/AdminProjects.tsx, frontend/src/pages/Admin/AdminFriendLinks.tsx, frontend/src/pages/Admin/AdminGuestbook.tsx, frontend/src/pages/Admin/AdminUsers.tsx, frontend/src/components/admin/AdminSidebar.tsx, frontend/src/components/admin/AdminTopbar.tsx</files>
  <read_first>frontend/src/components/layout/Sidebar.tsx, frontend/src/components/layout/Header.tsx, frontend/src/components/ui/card.tsx, frontend/src/components/ui/table.tsx, .planning/phases/02-content-management-core/02-UI-SPEC.md</read_first>
  <action>Implement a fixed desktop admin sidebar, mobile sheet behavior if needed, 64px top bar, and scrollable admin content canvas using existing UI primitives and lucide icons. Sidebar order must be 仪表盘, 文章, 笔记, 项目, 友链, 留言, 用户 per D-29. Overview must show content totals, draft counts, recent edits, and quick-create actions only per D-30; do not add traffic charts or analytics. Route page files must render functional empty/loading-ready shells that later plans replace with data-driven content, using the copy and color contract from `02-UI-SPEC.md`.</action>
  <verify>
    <automated>cd frontend; npm run build</automated>
  </verify>
  <acceptance_criteria>
    - Admin layout has sidebar + top bar + content area.
    - Overview has counts, drafts, recent edits, and quick-create actions.
    - All Phase 2 admin route page files compile.
  </acceptance_criteria>
  <done>Admin workspace shell is visible and follows UI-SPEC.</done>
</task>

<task type="auto">
  <name>Task 3: Add conditional admin entry to public sidebar</name>
  <files>frontend/src/components/layout/Sidebar.tsx</files>
  <read_first>frontend/src/components/layout/Sidebar.tsx, frontend/src/stores/authStore.ts, frontend/src/hooks/useCurrentIdentity.ts</read_first>
  <action>Add an admin navigation entry to the existing blog sidebar only for users whose current auth state identifies them as admin per D-09/D-26. Use a lucide icon, `aria-label`, and `title`. Preserve existing collapsed/mobile sidebar behavior and avoid changing public nav labels unrelated to this task.</action>
  <verify>
    <automated>cd frontend; npm run lint; npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `/admin` entry is conditionally rendered for admin users.
    - Visitors and non-admin users do not see the admin entry.
    - Existing sidebar collapsed behavior still compiles.
  </acceptance_criteria>
  <done>Admin entry is visible only to the blogger/admin.</done>
</task>
</tasks>

<verification>
- [ ] `cd frontend; npm run lint`
- [ ] `cd frontend; npm run build`
</verification>

<success_criteria>
- `/admin` route is protected and independent.
- Admin layout matches the UI-SPEC style reference.
- Public sidebar exposes admin entry only to admin users.
</success_criteria>

<output>
After completion, create `.planning/phases/02-content-management-core/02-05-SUMMARY.md`.
</output>
