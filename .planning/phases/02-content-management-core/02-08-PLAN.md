---
phase: 02-content-management-core
plan: "08"
type: execute
wave: 4
depends_on:
  - "02-05"
  - "02-07"
files_modified:
  - backend/src/modules/user/dto/admin-update-user.dto.ts
  - backend/src/modules/user/admin-user.controller.ts
  - backend/src/modules/user/user.service.ts
  - backend/src/modules/user/user.service.spec.ts
  - backend/src/modules/user/user.module.ts
  - frontend/src/api/adminManagement.ts
  - frontend/src/components/admin/management/AdminManagementTable.tsx
  - frontend/src/pages/Admin/AdminFriendLinks.tsx
  - frontend/src/pages/Admin/AdminGuestbook.tsx
  - frontend/src/pages/Admin/AdminUsers.tsx
autonomous: true
requirements:
  - ADMN-04
  - ADMN-05
  - ADMN-06
  - ADMN-01
  - ADMN-03
must_haves:
  truths:
    - "Admin can view users and adjust active/banned status through an admin-protected route."
    - "Friend-link admin page shows pending/approved/rejected management structure with clear empty states."
    - "Guestbook admin page is view/delete-oriented and does not introduce approval workflow."
    - "All admin pages pass combined backend/frontend verification and record the visual acceptance checklist for UI-SPEC review."
    - "Decision coverage: D-17: D-18: D-19: D-20: D-21: D-26: D-29: D-30:"
  artifacts:
    - path: "backend/src/modules/user/admin-user.controller.ts"
      provides: "Admin user management route boundary"
      contains: "@Roles('admin')"
    - path: "frontend/src/api/adminManagement.ts"
      provides: "Typed admin management API boundary"
      contains: "admin/users"
    - path: "frontend/src/pages/Admin/AdminFriendLinks.tsx"
      provides: "Friend-link admin management shell"
      contains: "pending"
    - path: "frontend/src/pages/Admin/AdminGuestbook.tsx"
      provides: "Guestbook admin management shell"
      contains: "删除"
    - path: "frontend/src/pages/Admin/AdminUsers.tsx"
      provides: "User management admin page"
      contains: "AdminManagementTable"
  key_links:
    - from: "frontend/src/pages/Admin/AdminUsers.tsx"
      to: "/api/v1/admin/users"
      via: "adminManagement API client through `apiClient`"
      pattern: "admin/users"
    - from: "backend/src/modules/user/admin-user.controller.ts"
      to: "backend/src/modules/user/user.service.ts"
      via: "controller calls list/update admin methods"
      pattern: "UserService"
---

<objective>
Finish the Phase 2 admin management shells and final acceptance gate.

Purpose: Friend links, guestbook, and users need real admin surfaces with safe action boundaries, while user management can use the existing user model.
Output: Admin user API, management API client, shared management table, friend-link/guestbook/user admin pages, and final automated verification.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-content-management-core/02-CONTEXT.md
@.planning/phases/02-content-management-core/02-RESEARCH.md
@.planning/phases/02-content-management-core/02-UI-SPEC.md
@.planning/phases/02-content-management-core/02-VALIDATION.md
@.planning/phases/02-content-management-core/02-05-SUMMARY.md
@.planning/phases/02-content-management-core/02-07-SUMMARY.md
@backend/src/modules/user/user.service.ts
@backend/src/modules/user/user.module.ts
@backend/src/modules/user/schemas/user.schema.ts
@frontend/src/api/client.ts
@frontend/src/components/ui/table.tsx
@frontend/src/components/ui/dialog.tsx
</context>

<source_audit>
GOAL: completes the admin management scope around the core content workspace.
REQ: covers ADMN-04, ADMN-05, ADMN-06 and final acceptance checks for ADMN-01/ADMN-03.
RESEARCH: follows the admin shell boundary and existing user model boundary.
CONTEXT: implements D-17, D-18, D-19, D-20, D-21, D-26, D-29, and D-30.
UI-SPEC: implements friend-link, guestbook, user shell contracts and final visual acceptance.
</source_audit>

<decision_coverage>
D-17: friend links, guestbook, and users receive real admin shells with empty states, filters, action affordances, and API boundaries.
D-18: guestbook management is view/delete-oriented.
D-19: friend-link admin shows pending, approved, and rejected review structure.
D-20: user management reflects the existing admin/user boundary and ban/unban affordances.
D-21: guestbook approval workflow is excluded.
D-26: management writes are admin-only.
D-29: management pages sit in the content-first admin sidebar order.
D-30: final verification preserves lightweight overview scope and excludes analytics dashboard content.
</decision_coverage>

<threat_model>
STRIDE register:
- T08-01 Information disclosure: user management exposes password hashes. Mitigation: user service admin list/detail methods always select `-password`.
- T08-02 Elevation of privilege: non-admin disables users or changes roles. Mitigation: admin user controller uses `@Roles('admin')` and validates role/status allowlists.
- T08-03 Scope creep: guestbook approval workflow appears despite user decision. Mitigation: guestbook page actions are view/delete-oriented only per D-18/D-21.
</threat_model>

<tasks>
<task type="auto">
  <name>Task 1: Add admin user management API</name>
  <files>backend/src/modules/user/dto/admin-update-user.dto.ts, backend/src/modules/user/admin-user.controller.ts, backend/src/modules/user/user.service.ts, backend/src/modules/user/user.service.spec.ts, backend/src/modules/user/user.module.ts</files>
  <read_first>backend/src/modules/user/user.service.ts, backend/src/modules/user/user.module.ts, backend/src/modules/user/schemas/user.schema.ts, backend/src/modules/upload/upload.controller.ts</read_first>
  <action>Add `AdminUserController` under `admin/users` with `GET /admin/users` and `PATCH /admin/users/:id` guarded by `@Roles('admin')`. Add `AdminUpdateUserDto` that allows status `active|banned` and role `admin|user` only. Extend `UserService` with admin list/update methods that always exclude `password`. Add Jest tests for password exclusion, status update, role update, and invalid status rejection through DTO validation where practical. This implements D-20 and D-26 without adding public registration.</action>
  <verify>
    <automated>cd backend; npm test -- --runInBand user.service.spec.ts; npm run build</automated>
  </verify>
  <acceptance_criteria>
    - Admin user controller uses `@Roles('admin')`.
    - User list/update responses exclude password.
    - Service tests cover status and role updates.
  </acceptance_criteria>
  <done>Admin user management API is role-protected and safe to consume.</done>
</task>

<task type="auto">
  <name>Task 2: Implement friend-link, guestbook, and user admin pages</name>
  <files>frontend/src/api/adminManagement.ts, frontend/src/components/admin/management/AdminManagementTable.tsx, frontend/src/pages/Admin/AdminFriendLinks.tsx, frontend/src/pages/Admin/AdminGuestbook.tsx, frontend/src/pages/Admin/AdminUsers.tsx</files>
  <read_first>frontend/src/api/client.ts, frontend/src/pages/Admin/AdminArticles.tsx, frontend/src/components/admin/content/AdminContentTable.tsx, frontend/src/components/ui/table.tsx, frontend/src/components/ui/dialog.tsx, .planning/phases/02-content-management-core/02-UI-SPEC.md</read_first>
  <action>Create typed admin management API helpers for users and page-local data contracts for friend links and guestbook. Build a reusable `AdminManagementTable` for status filters, search, empty states, and row actions. Implement friend-link page with pending/approved/rejected structure per D-19, guestbook page with view/delete-oriented actions and no approval UI per D-18/D-21, and user page wired to `/admin/users` with ban/unban and role controls per D-20. Use existing UI components and UI-SPEC copy; keep public friend-link application, public guestbook posting, guestbook replies, and public registration outside the implementation.</action>
  <verify>
    <automated>cd frontend; npm run lint; npm run build</automated>
  </verify>
  <acceptance_criteria>
    - Friend-link page shows pending/approved/rejected filters and empty states.
    - Guestbook page does not include approval controls.
    - Users page calls `admin/users` through `apiClient`.
  </acceptance_criteria>
  <done>Management shells are route-complete, visually consistent, and aligned with Phase 2 scope.</done>
</task>

<task type="auto">
  <name>Task 3: Run combined phase verification</name>
  <files>backend/src/modules/article/article.service.spec.ts, backend/src/modules/note/note.service.spec.ts, backend/src/modules/project/project.service.spec.ts, backend/src/modules/user/user.service.spec.ts, frontend/src/pages/Admin/AdminLayout.tsx</files>
  <read_first>.planning/phases/02-content-management-core/02-VALIDATION.md, .planning/phases/02-content-management-core/02-UI-SPEC.md</read_first>
  <action>Run the complete Phase 2 verification suite from `02-VALIDATION.md`. Fix only failures caused by Phase 2 work, preserving unrelated user changes. Confirm backend tests/build and frontend lint/build are green, then record in the summary that `/admin` still needs visual acceptance against `02-UI-SPEC.md` before phase closeout.</action>
  <verify>
    <automated>cd backend; npm test -- --runInBand; npm run test:e2e -- --runInBand; cd ../frontend; npm run lint; npm run build</automated>
  </verify>
  <acceptance_criteria>
    - Backend unit tests pass.
    - Backend e2e tests pass or are updated to match the current `/api/v1` app contract if the legacy root test is stale.
    - Frontend lint and build pass.
  </acceptance_criteria>
  <done>Automated Phase 2 verification is green.</done>
</task>

</tasks>

<verification>
- [ ] `cd backend; npm test -- --runInBand`
- [ ] `cd backend; npm run test:e2e -- --runInBand`
- [ ] `cd frontend; npm run lint`
- [ ] `cd frontend; npm run build`
- [ ] Summary records visual acceptance checklist against `02-UI-SPEC.md`
</verification>

<success_criteria>
- Friend-link, guestbook, and user admin pages are usable within the Phase 2 scope.
- User management API is admin-only and never returns password hashes.
- Guestbook management has no approval workflow.
- Full automated and human visual acceptance checks pass.
</success_criteria>

<output>
After completion, create `.planning/phases/02-content-management-core/02-08-SUMMARY.md`.
</output>
