---
phase: 01-infrastructure
plan: 04
type: execute
wave: 4
depends_on: ["01-01", "01-02", "01-03"]
files_modified:
  - .env.example
  - README.md
  - backend/Dockerfile
  - backend/src/app.module.ts
  - backend/src/config/jwt.config.ts
  - backend/src/modules/health/health.controller.ts
  - backend/src/modules/health/health.module.ts
  - backend/src/modules/health/health.service.ts
  - docker-compose.yml
  - frontend/Dockerfile
  - frontend/nginx.conf
autonomous: true
requirements: [INFR-01, INFR-02, INFR-03, INFR-04, AUTH-02, AUTH-03, AUTH-04, AUTH-05]
user_setup: []
must_haves:
  truths:
    - "D-17: `docker-compose up -d` can start frontend, backend, MongoDB, Redis, and Elasticsearch for local validation."
    - "D-18: All runtime services have healthchecks or startup availability checks."
    - "D-19: Backend waits for or retries unhealthy dependencies instead of racing blindly."
    - "D-20: Production/compose startup fails when `JWT_SECRET` or sensitive config uses placeholder values."
    - "D-21: README quickstart documents env vars, owner seed command, compose startup, health checks, and troubleshooting."
    - "D-22: Phase 1 stops at local full-stack reliability, not production hardening."
  artifacts:
    - path: "backend/src/modules/health/health.controller.ts"
      provides: "Backend health endpoint"
      contains: "health"
    - path: "docker-compose.yml"
      provides: "Full-stack service healthchecks"
      contains: "service_healthy"
    - path: "README.md"
      provides: "Phase 1 quickstart"
      contains: "seed:owner"
  key_links:
    - from: "docker-compose.yml"
      to: "backend/src/modules/health/health.controller.ts"
      via: "backend healthcheck URL"
      pattern: "api/v1/health"
    - from: "frontend/nginx.conf"
      to: "uploads volume"
      via: "`/uploads` static serving"
      pattern: "/uploads"
---

<objective>
Harden the local full-stack deployment path and document it.

Purpose: Make the Phase 1 skeleton repeatable from a clean checkout with explicit secrets, health checks, and quickstart instructions.
Output: Health module, Compose healthchecks and dependency conditions, production placeholder-secret failure, Dockerfile adjustments if needed, and README quickstart.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-infrastructure/01-CONTEXT.md
@.planning/phases/01-infrastructure/01-RESEARCH.md
@docker-compose.yml
@.env.example
@backend/Dockerfile
@frontend/Dockerfile
@frontend/nginx.conf
@backend/src/app.module.ts
@backend/src/config/jwt.config.ts
</context>

<threat_model>
Trust boundaries: operator environment to Docker Compose, Docker network between services, public Nginx to backend proxy, backend config to JWT signing.

STRIDE register:
- T-01 Weak placeholder JWT secret used in compose/production. Severity: high. Mitigation: fail startup when placeholder or missing secret is detected outside development.
- T-02 False-positive deployment readiness. Severity: medium. Mitigation: service healthchecks and `depends_on.condition: service_healthy`.
- T-03 Leaking sensitive values in docs/logs. Severity: medium. Mitigation: README uses variable names and example placeholders only, never real secrets.
- T-04 Exposing Elasticsearch with security disabled beyond local network. Severity: medium. Mitigation: document local-only intent; production ES hardening remains out of Phase 1.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Add backend health endpoint covering dependencies</name>
  <files>backend/src/modules/health/health.controller.ts, backend/src/modules/health/health.module.ts, backend/src/modules/health/health.service.ts, backend/src/app.module.ts</files>
  <read_first>backend/src/app.module.ts, backend/src/shared/redis/redis.service.ts, backend/src/shared/elasticsearch/elasticsearch.service.ts, backend/src/config/database.config.ts, backend/src/modules/article/article.controller.ts</read_first>
  <action>
    Add `HealthModule` with a public `GET /api/v1/health` endpoint.
    The health service should check application status and dependency availability for MongoDB, Redis, and Elasticsearch using existing injected clients/services where practical.
    Return normalized data through `TransformInterceptor`, for example `{ status: 'ok', services: { mongodb: 'ok', redis: 'ok', elasticsearch: 'ok' } }`.
    Keep the endpoint public so Docker healthchecks do not require auth.
    If a dependency check is temporarily unavailable at implementation time, return degraded status clearly and make verification catch it.
  </action>
  <verify>cd backend && npm run build</verify>
  <acceptance_criteria>
    - `health.controller.ts` exposes `GET /health` under the global prefix.
    - Health endpoint is marked public.
    - Health response includes MongoDB, Redis, and Elasticsearch status keys.
    - Backend build passes.
  </acceptance_criteria>
  <done>Backend exposes a Docker-friendly health endpoint.</done>
</task>

<task type="auto">
  <name>Task 2: Make Compose wait for healthy runtime services</name>
  <files>docker-compose.yml, backend/Dockerfile, frontend/Dockerfile, frontend/nginx.conf</files>
  <read_first>docker-compose.yml, backend/Dockerfile, frontend/Dockerfile, frontend/nginx.conf</read_first>
  <action>
    Add healthchecks for MongoDB, Redis, Elasticsearch, backend, and frontend/Nginx.
    Use image-appropriate checks: Redis `redis-cli ping`, MongoDB `mongosh --eval "db.adminCommand('ping')"`, Elasticsearch HTTP health, backend `/api/v1/health`, and frontend root or a dedicated Nginx health path.
    If backend or frontend image lacks a needed healthcheck tool, adjust the Dockerfile with the smallest Alpine package install or use Node 20 built-in `fetch` for backend.
    Replace short `depends_on` where needed with dependency conditions so backend waits for MongoDB/Redis/Elasticsearch health and frontend waits for backend health.
    Preserve the existing `uploads` volume mapping.
  </action>
  <verify>docker compose config</verify>
  <acceptance_criteria>
    - `docker-compose.yml` contains healthchecks for all five runtime services.
    - `docker-compose.yml` contains `condition: service_healthy` for backend dependencies.
    - `docker-compose.yml` preserves `uploads` volume mappings.
    - `docker compose config` succeeds.
  </acceptance_criteria>
  <done>Compose startup has readiness checks instead of order-only dependency wiring.</done>
</task>

<task type="auto">
  <name>Task 3: Fail fast on production placeholder secrets</name>
  <files>backend/src/config/jwt.config.ts, .env.example, docker-compose.yml</files>
  <read_first>backend/src/config/jwt.config.ts, .env.example, docker-compose.yml, docs/superpowers/specs/2026-05-19-blog-design.md</read_first>
  <action>
    Update JWT config so production/compose mode refuses missing, `default-secret`, or `change-me` style JWT secrets.
    Keep `.env.example` as a template with obvious placeholder values, but ensure runtime compose does not silently use `${JWT_SECRET:-change-me-to-a-secure-random-string}`.
    Update compose environment to require explicit `JWT_SECRET` from the operator's environment or `.env`.
    Keep error messages clear without printing secret values.
  </action>
  <verify>cd backend && npm run build</verify>
  <acceptance_criteria>
    - `jwt.config.ts` throws or fails clearly for placeholder secrets when `NODE_ENV=production`.
    - `docker-compose.yml` no longer supplies a safe-looking default JWT secret fallback.
    - `.env.example` still documents `JWT_SECRET`.
    - Backend build passes.
  </acceptance_criteria>
  <done>Weak production secrets cannot run silently.</done>
</task>

<task type="auto">
  <name>Task 4: Document Phase 1 quickstart and verification</name>
  <files>README.md</files>
  <read_first>README.md, .env.example, docker-compose.yml, backend/package.json, .planning/phases/01-infrastructure/01-CONTEXT.md</read_first>
  <action>
    Update or create root `README.md` with a Phase 1 quickstart.
    Include required environment variables, how to create `.env`, how to run the owner seed command, how to start with `docker compose up -d`, how to inspect health status, how to verify backend `/api/v1/health`, how to verify uploads are served, and common troubleshooting notes for MongoDB/Redis/Elasticsearch startup.
    Mention that public visitor registration is intentionally not available and that visitor image uploads are deferred.
    Do not include real secrets.
  </action>
  <verify>Select-String -Path README.md -Pattern "docker compose up -d","seed:owner","/api/v1/health"</verify>
  <acceptance_criteria>
    - README contains `docker compose up -d`.
    - README contains `seed:owner`.
    - README contains `/api/v1/health`.
    - README states visitor registration is not public in Phase 1.
  </acceptance_criteria>
  <done>Future execution and local operators have a clear startup and verification path.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `docker compose config`
- [ ] `cd backend && npm run build`
- [ ] `cd frontend && npm run build`
- [ ] `docker compose up -d` starts all services.
- [ ] `docker compose ps` shows all runtime services healthy.
- [ ] `curl http://localhost/api/v1/health` or `curl http://localhost:3000/api/v1/health` returns normalized healthy data.
- [ ] README quickstart reflects the actual commands.
</verification>

<success_criteria>

- All tasks completed.
- Compose config is valid.
- Runtime services have meaningful healthchecks.
- Production placeholder JWT secret behavior fails fast.
- Quickstart docs match the implemented commands.
- Full-stack startup can be verified locally.
</success_criteria>

<output>
After completion, create `.planning/phases/01-infrastructure/01-04-SUMMARY.md`
</output>
