---
phase: 02.1-editor-home-api-gap-closure
verified: 2026-05-23T00:00:00Z
status: human_needed
score: 13/13 must-haves verified
overrides_applied: 0
overrides: []
re_verification: false
gaps: []
deferred: []
human_verification:
  - test: "Open /admin/articles/new and /admin/notes/new in light and dark mode; verify editor background and text are readable, editor surface matches admin card"
    expected: "Editor text and placeholder are readable; editor surface blends with admin card, not a disconnected black panel"
    why_human: "Visual rendering and theme integration must be verified in a browser; no automated visual-diff tool available"
  - test: "Exercise the full block editing chain: type plain text, use slash menu for heading/list/quote/code/table, insert image, paste Markdown ('## Heading\n\n**bold** text\n\n- item 1\n- item 2'), delete a table via the delete-table control, save as draft, reopen, and preview"
    expected: "All block types can be created, edited, deleted; Markdown paste produces structured blocks; saved content reopens correctly; preview shows rendered content without editor chrome"
    why_human: "BlockNote editor interaction, clipboard handling, and serialization roundtrip require browser runtime with real DOM events"
  - test: "Create a mixed-block article (headings, lists, code, table, image), save, navigate away, reopen from admin list, verify Edit view and Preview match saved content"
    expected: "Saved content persists across navigation; preview reflects exact block structure; no data loss on roundtrip"
    why_human: "Editor state persistence and serialization fidelity require browser testing with real backend API"
  - test: "With published articles in database: visit home page, verify LatestArticles section shows real article titles/summaries/covers/dates from the API"
    expected: "Real article data renders; no old mock article titles appear; cover images or fallback placeholders display correctly"
    why_human: "Requires backend with published article data; API integration path needs live data verification"
  - test: "With zero published articles: verify LatestArticles shows empty state ('还没有文章，敬请期待') without collapsing the layout"
    expected: "Section remains visible at stable height with compact empty message; '查看全部' link still visible"
    why_human: "Empty state rendering depends on actual API response; cannot simulate reliably without backend"
  - test: "With zero published projects: verify FeaturedProjects section shows empty state ('项目正在整理中') without silently disappearing"
    expected: "Section remains visible with compact empty message; layout stable at same height as data state; content below does not jump"
    why_human: "Empty state replacement of prior 'return null' behavior requires actual API response verification"
  - test: "With project API simulated failure: verify FeaturedProjects section shows error fallback ('项目加载失败，请稍后重试') without crashing or vanishing"
    expected: "Section visible with amber-tinted error message; '查看全部' link visible; home page otherwise functional"
    why_human: "Error state UI depends on actual API failure path; control flow differentiation (error vs empty vs loading) needs visual verification"
  - test: "Verify project form is rendered for kind='project' in AdminContentEditor and does NOT render the BlockNote editor"
    expected: "Project form with project-specific fields (status, tech stack, screenshots, repo URL, demo URL) renders; BlockNote editor is absent"
    why_human: "Conditional component rendering needs browser verification to confirm the 'kind === project' branch is active"
---

# Phase 02.1: Editor and Home API Gap Closure Verification Report

**Phase Goal:** 修复后台内容编辑器的核心编辑能力，并补齐首页项目空数据兜底与最新文章真实接口接入；优先解决编辑器黑底、只能输入文字等当前阻塞问题。

**Verified:** 2026-05-23
**Status:** human_needed
**Re-verification:** No -- initial verification

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | The article/note BlockNote editor must not render as an unreadable black surface in either light or dark mode | VERIFIED | `ContentBlockEditor.tsx` imports `useThemeStore` (line 16), reads `isDark` (line 98), passes `theme={isDark ? 'dark' : 'light'}` to `BlockNoteViewRaw` (line 148). `index.css` lines 109-135 add `@layer components` overrides scoped to `.admin-block-editor .bn-root`, mapping 16 BlockNote CSS variables to project semantic tokens (`hsl(var(--card))`, `hsl(var(--foreground))`, etc.) |
| 2 | The editor must expose real block editing affordances: slash menu, side menu, table handles, image upload, formatting toolbar, link toolbar, and table deletion | VERIFIED | `ContentBlockEditor.tsx` passes `formattingToolbar={false}` (custom), `linkToolbar`, `slashMenu`, `sideMenu`, `tableHandles` to `BlockNoteViewRaw` (lines 153-157). Custom `AdminFormattingToolbar` with `DeleteTableButton` (lines 46-81). `uploadFile` wired via `onUploadImage` prop (lines 103-108). `pasteHandler` configured (lines 117-122) |
| 3 | The editor must support creating, editing, deleting, saving, reopening, and previewing headings, paragraphs, lists, quotes, code blocks, images, and tables | VERIFIED | `useCreateBlockNote` with `initialContent` for reopen, `editable` prop, `onChange` for save (lines 100-125). All default block types enabled (no restrictions). `ContentBlockPreview.tsx` uses `blocksToFullHTML` for preview (line 28). Editor body state managed by `AdminContentEditor.tsx` via `value`/`onChange` props (lines 301-306) |
| 4 | Markdown paste must produce structured blocks for common Markdown input, not a single plain text blob | VERIFIED | `ContentBlockEditor.tsx` lines 117-122: `pasteHandler` callback with `prioritizeMarkdownOverHTML: true` and `plainTextAsMarkdown: true` |
| 5 | Preview must render exported HTML/prose output from the saved block content, not a read-only editor shell | VERIFIED | `ContentBlockPreview.tsx` renders via `editor.blocksToFullHTML(initialContent)` through `dangerouslySetInnerHTML` (line 35) inside a prose-styled div with full Tailwind styling for headings, lists, code, tables, and dark mode (line 34). No read-only BlockNote editor shell rendered |
| 6 | LatestArticles must fetch real published article data from GET /api/v1/articles | VERIFIED | `publicContent.ts` exports `fetchPublicArticles` calling `apiClient.get('/articles', { params })` (line 63). `usePublicContent.ts` exports `usePublicArticles` hook with 5-min staleTime (lines 34-45). `LatestArticles.tsx` calls `usePublicArticles({ pageSize: 3 })` (line 75) |
| 7 | LatestArticles must not keep the current hardcoded mock article array as the normal data source | VERIFIED | `LatestArticles.tsx` has no `Article` type declaration, no `articles` array constant, no hardcoded object literals for article data. All data flows from `data?.items ?? []` (line 76) mapped through `mapArticle` helper |
| 8 | LatestArticles must handle loading, empty, and error states without breaking the home layout | VERIFIED | Loading: three `SkeletonCard` components in `ShowcaseGrid` at `h-[156px]`. Error: centered "加载失败，请稍后再试" message at `h-[156px]`. Empty: centered "还没有文章，敬请期待" message at `h-[156px]`. Section shell unchanged across all states |
| 9 | Read count display must not fake historical analytics; use an optional API field when present, otherwise use an honest zero/placeholder until DATA-01 adds analytics | VERIFIED | `mapArticle` line 44: `const views = article.viewCount ?? article.readCount ?? article.views ?? 0`. Honest zero default; no `1.2k` or other fabricated values |
| 10 | FeaturedProjects must not silently disappear when the public projects API returns an empty list | VERIFIED | Old `return null` removed. Empty state renders `FeaturedProjectsShell` with centered "项目正在整理中" message in a `h-[156px]` dashed-border box (lines 128-137) |
| 11 | Empty projects are the current bug state; hiding the whole module is not an acceptable target behavior | VERIFIED | No `return null` exists in the file (verified by grep). All states return visible content within `FeaturedProjectsShell` |
| 12 | FeaturedProjects must preserve the home layout in loading, empty, and error states | VERIFIED | `FeaturedProjectsShell` extracted (lines 58-87) to share glass-morphism section shell. Loading: skeleton cards in ShowcaseGrid at `h-[156px]`. Error: amber-tinted fallback at `h-[156px]`. Empty: slate-tinted fallback at `h-[156px]`. Error state checked before empty/data to prevent misleading display |
| 13 | FeaturedProjects must keep using real project data from GET /api/v1/projects when published projects exist | VERIFIED | Calls `usePublicProjects({ pageSize: 10 })` (line 90). Maps API data through `mapToDisplayProject` (line 92). Renders real cards via `visibleProjects` (lines 141-193). Chinese status labels preserved. Cover fallback gradients preserved |

**Score:** 13/13 truths verified

### Deferred Items

None. All required functionality for this phase is implemented in the codebase. No items were deferred to later phases.

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `frontend/src/components/admin/editor/ContentBlockEditor.tsx` | Admin BlockNote adapter with explicit theme, block UI controls, upload, markdown paste, table delete | VERIFIED | 165 lines. Contains `BlockNoteViewRaw`, `useThemeStore`, `DeleteTableButton`, `AdminFormattingToolbar`, `pasteHandler`, `uploadFile`, theme prop passthrough. Wrapper has `admin-block-editor` class |
| `frontend/src/components/admin/editor/ContentBlockPreview.tsx` | Preview renderer using `blocksToFullHTML` for HTML output | VERIFIED | 38 lines. Uses `blocksToFullHTML(initialContent)` with reactive `serializedValue` dependency. Renders via `dangerouslySetInnerHTML` with prose + Tailwind dark mode styling |
| `frontend/src/index.css` | Project-level BlockNote CSS variable overrides scoped to `.admin-block-editor` | VERIFIED | Lines 109-135. `@layer components` with `.admin-block-editor .bn-root` mapping 16 BlockNote variables to project tokens (`--card`, `--foreground`, `--popover`, `--accent`, `--muted`, `--border`). `.admin-block-editor .bn-editor` for editor surface |
| `frontend/src/components/admin/content/AdminContentEditor.tsx` | Admin content editor shell with BlockNote integration for articles/notes, ProjectForm for projects | VERIFIED | 505 lines. Contains `ContentBlockEditor` import (line 24), wired with `value={body}`, `onChange={setBody}`, `onUploadImage` (lines 301-306). `kind === 'project'` renders `ProjectForm` (line 267). Preview dialog uses `ContentBlockPreview` with `value={body}` |
| `frontend/src/api/publicContent.ts` | Public article API client alongside existing project client | VERIFIED | 66 lines. Exports `PublicArticle` interface (lines 36-51), `fetchPublicArticles` (lines 53-65) calling `apiClient.get('/articles', { params })`. Reuses existing `PublicListResponse<T>` |
| `frontend/src/hooks/usePublicContent.ts` | React Query hook and query keys for public articles | VERIFIED | 49 lines. Exports `publicContentKeys.articles.all` and `articles.list(params)`, `usePublicArticles(params?)` with 5-min staleTime (lines 34-45). Re-exports `PublicArticle` and `PublicProject` types (line 48) |
| `frontend/src/components/home/LatestArticles.tsx` | Home latest article section backed by live API data | VERIFIED | 195 lines. Uses `usePublicArticles({ pageSize: 3 })`. Three explicit states (loading/skeleton, error, empty) via IIFE in `sectionBody`. `mapArticle` helper maps API fields. Category badges use deterministic string-hash gradient. No hardcoded article data |
| `frontend/src/components/home/FeaturedProjects.tsx` | Featured project section with stable empty/error fallbacks | VERIFIED | 193 lines. Extracted `FeaturedProjectsShell` eliminated 4x duplication. Error state (amber) checked before empty (slate). Loading state with skeleton cards. Empty state replaces old `return null`. Real data rendering preserved with `ShowcaseGrid` |
| `frontend/src/components/home/ProjectSubscribeSection.tsx` | Home composition wrapper for FeaturedProjects | VERIFIED | 9 lines. Renders `<FeaturedProjects />` inside a flex column div. Does not duplicate rendering logic |
| `frontend/src/pages/Admin/AdminArticles.tsx` | Admin articles page with editor and table views | VERIFIED (reviewed) | Uses `AdminContentEditor` with `kind="article"`. Routes: `/admin/articles/new` (create), `/admin/articles/:id/edit` (edit). No changes needed for this phase |
| `frontend/src/pages/Admin/AdminNotes.tsx` | Admin notes page with editor and table views | VERIFIED (reviewed) | Uses `AdminContentEditor` with `kind="note"`. Routes: `/admin/notes/new` (create), `/admin/notes/:id/edit` (edit). No changes needed for this phase |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `themeStore.ts` | `ContentBlockEditor.tsx` | `isDark` drives `BlockNoteViewRaw` theme prop | WIRED | `useThemeStore` imported at line 16, `isDark` read at line 98, passed as `theme={isDark ? 'dark' : 'light'}` at line 148 |
| `AdminContentEditor.tsx` | `ContentBlockEditor.tsx` | Article/note editor body field | WIRED | `ContentBlockEditor` imported at line 24, rendered with `value={body}`, `onChange={setBody}`, `onUploadImage={handleBodyImageUpload}` at lines 301-306 |
| `LatestArticles.tsx` | `GET /api/v1/articles` | `usePublicArticles` -> `fetchPublicArticles` -> `apiClient.get` | WIRED | `usePublicArticles` called at line 75; `fetchPublicArticles` calls `apiClient.get('/articles', ...)` at line 63 of `publicContent.ts`; data flows through `data?.items` to `articles` mapped array |
| `FeaturedProjects.tsx` | `usePublicContent.ts` | `usePublicProjects` hook | WIRED | `usePublicProjects` imported at line 8, called at line 90 with `{ pageSize: 10 }`; data flows through `data?.items` to `displayProjects` mapped array |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|----------|--------------|--------|--------------------|--------|
| `LatestArticles.tsx` | `data?.items` | `usePublicArticles({ pageSize: 3 })` -> `fetchPublicArticles` -> `apiClient.get('/articles', ...)` | FLOWING | Backend `ArticleService.findAllPublic` filters to published, non-deleted; API returns `PublicListResponse<PublicArticle>` |
| `FeaturedProjects.tsx` | `data?.items` | `usePublicProjects({ pageSize: 10 })` -> `fetchPublicProjects` -> `apiClient.get('/projects', ...)` | FLOWING | Backend `ProjectService.findAllPublic` filters to published, non-deleted; API returns `PublicListResponse<PublicProject>` |
| `ContentBlockPreview.tsx` | `value` (prop) | Passed from `AdminContentEditor.tsx` body state, populated by `ContentBlockEditor` onChange | FLOWING | Editor onChange fires on every edit; blocks serialized to JSON; preview re-renders via `serializedValue` dependency on `useCreateBlockNote` |

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|-------------|-------------|--------|----------|
| ADMN-03 | 02.1-01-PLAN.md | 后台支持文章/项目/笔记的完整 CRUD | SATISFIED | Editor theme fix + block editing chain verified ensures CRUD editing is functional. `AdminContentEditor` handles create/edit flow with save draft, publish, soft delete. Article, note use BlockNote; project uses ProjectForm |
| ARTC-07 | 02.1-01-PLAN.md, 02.1-02-PLAN.md | 后端文章 CRUD API 完整，支持富文本/Markdown 内容存储 | SATISFIED | `fetchPublicArticles` in `publicContent.ts` consumes `GET /api/v1/articles`. Editor stores block content as JSON. Backend article service already complete from Phase 2 |
| HOME-02 | 02.1-02-PLAN.md | 最新文章列表展示（标题、摘要、标签、阅读数、发布时间） | SATISFIED | `LatestArticles.tsx` renders title, excerpt, category badge (with deterministic gradient), date (YYYY/MM/DD), and views from live API. Uses `usePublicArticles({ pageSize: 3 })` |
| HOME-03 | 02.1-03-PLAN.md | 精选项目卡片展示（封面、名称、技术栈、状态） | SATISFIED | `FeaturedProjects.tsx` renders cover/fallback, name, description, status badge (Chinese labels), and tech stack from live API via `usePublicProjects({ pageSize: 10 })`. Empty/error fallbacks no longer silently hide the section |
| HOME-09 | 02.1-02-PLAN.md, 02.1-03-PLAN.md | 首页所有数据从后端 API 动态获取，非静态写死 | SATISFIED | Both `LatestArticles` and `FeaturedProjects` fetch data from public APIs. No hardcoded mock data remains in either component. Empty/loading/error states are explicit |

**REQUIREMENTS.md traceability discrepancy:** The REQUIREMENTS.md traceability table (line 166) maps HOME-02 to Phase 5 (Pending), but the ROADMAP (line 100) lists HOME-02 among Phase 02.1 requirements, and the codebase evidence confirms it was implemented in this phase. The REQUIREMENTS.md table should be updated to reflect `HOME-02 | Phase 02.1 | Complete`.

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| `ContentBlockEditor.tsx` | 59 | `return null` -- conditional render for `DeleteTableButton` | INFO | This is intentional: the delete-table button is only visible when a table block is selected. Not a stub. |
| `ContentBlockEditor.tsx` | 116 | `as any` cast on BlockNote dictionary type | INFO | Intentional adapter-boundary cast with eslint-disable comment. Not a quality gap. |
| `ContentBlockPreview.tsx` | 35 | `dangerouslySetInnerHTML` | MITIGATED | Threat T-02.1-01-01 dispositioned as mitigate in plan. HTML comes from `blocksToFullHTML` which is BlockNote-generated, not raw user input. Consistent with threat model. |

No TBD, FIXME, XXX, TODO, HACK, or PLACEHOLDER markers found in any modified file. No empty implementations or console.log stubs detected.

### Behavioral Spot-Checks

| Behavior | Check | Result | Status |
|----------|-------|--------|--------|
| Theme wiring | `grep -n "theme=" ContentBlockEditor.tsx` found `theme={isDark ? 'dark' : 'light'}` at line 148 | Match confirmed | PASS |
| CSS overrides present | `.admin-block-editor .bn-root` block in `index.css` lines 109-129 with 16 variable mappings | 16 variable mappings confirmed | PASS |
| fetchPublicArticles exists | `publicContent.ts` contains `fetchPublicArticles` calling `apiClient.get('/articles', ...)` | Match confirmed at line 63 | PASS |
| usePublicArticles exists | `usePublicContent.ts` contains `usePublicArticles` with 5-min staleTime | Match confirmed at line 34 | PASS |
| No hardcoded article data | `grep -rn "articles.*=.*\[" LatestArticles.tsx` returned no matches | No hardcoded data confirmed | PASS |
| No `return null` in FeaturedProjects | `grep -rn "return null" FeaturedProjects.tsx` returned no matches | No silent disappearing confirmed | PASS |
| Commit integrity | All 6 mentioned commits (10698de, f605836, cc8b129, 0c7bff6, cea2973, b373b60) verified in git log | All commits exist | PASS |

### Probe Execution

Step 7c: SKIPPED. No probe scripts (`scripts/*/tests/probe-*.sh`) found in the project. Plans and summaries do not reference probe-based verification.

### Human Verification Required

These items require browser runtime and cannot be verified through static code analysis alone.

#### 1. Editor Theme Visual Check (Light/Dark Mode)
**Test:** Open `/admin/articles/new` and `/admin/notes/new` in both light and dark mode.
**Expected:** Editor text and placeholder remain readable. Editor surface visually matches the admin card background. No disconnected black panel appears in either mode.
**Why human:** Visual rendering and CSS cascade interaction with BlockNote's built-in styles require a real browser.

#### 2. Full Block Editing Chain
**Test:** Create a new article. Type plain text. Use slash menu to create heading, bullet list, numbered list, quote, code block, image, and table blocks. Select a table cell and use the delete-table control. Paste `## Heading\n\n**bold** text\n\n- item 1\n- item 2` -- verify structured blocks appear. Save as draft. Navigate away and reopen from admin articles list. Verify Edit view and Preview show saved content.
**Expected:** All block types create/edit/delete correctly. Markdown paste produces structured blocks (h2 heading, bold text, unordered list items). Save/reopen preserves full block structure. Preview displays rendered content without editor controls.
**Why human:** BlockNote editor interaction, clipboard paste handling, and JSON serialization roundtrip require real DOM events and a running backend.

#### 3. Save/Reopen/Preview Fidelity
**Test:** Create a mixed-block article with headings, lists, code blocks, a table, and an image. Save. Navigate to article list, reopen the article. Verify Edit view matches saved content. Click "预览内容" button. Close and reopen preview.
**Expected:** Saved content persists exactly across navigation. Preview shows formatted HTML output matching the block structure. No data loss on roundtrip. Preview HTML includes tables with borders, code blocks with dark background, heading hierarchy, list styling.
**Why human:** Editor state persistence, serialization fidelity, and preview HTML output require browser testing against actual backend storage.

#### 4. LatestArticles Live API Data
**Test:** With published articles present in the database: visit the home page. Verify the "最新文章" section displays real article titles, summaries, cover images (or fallback placeholders), category badges, dates in YYYY/MM/DD format, and view counts.
**Expected:** Real article data from the API renders. No old mock article titles (previously hardcoded) appear. Cover images display with gradient fallback when `coverImage` is absent. Category badges show with deterministic colors.
**Why human:** Requires backend with published article data; API integration path needs live data verification.

#### 5. LatestArticles Empty State
**Test:** With zero published articles (empty `items` array from API): verify the "最新文章" section shows "还没有文章，敬请期待" without collapsing or shifting the layout.
**Expected:** Section remains at stable `h-[156px]`. Compact empty message visible. "查看全部" link still present. Content below the section does not jump.
**Why human:** Empty state rendering depends on actual API response; cannot simulate reliably without backend.

#### 6. FeaturedProjects Empty State
**Test:** With zero published projects (empty `items` array from API): verify the "精选项目" section shows "项目正在整理中" without silently disappearing from the page.
**Expected:** Section remains visible with compact empty message at stable height. Layout does not collapse or reflow. Content below does not jump. Message uses slate-tinted border for distinction from error state.
**Why human:** The old `return null` bug was a runtime behavior; must verify the fix at runtime with actual empty API response.

#### 7. FeaturedProjects Error State
**Test:** Simulate project API failure (network error or 500 response). Verify the "精选项目" section shows "项目加载失败，请稍后重试" without crashing the home page or redirecting.
**Expected:** Section visible with amber-tinted error message. "查看全部" link visible. Home page otherwise functional. Error state distinct from empty state (amber vs. slate tinting).
**Why human:** Error state UI depends on actual API failure path; control flow differentiation and visual distinction between error/empty/loading needs browser verification.

#### 8. Project Form (Not BlockNote Editor)
**Test:** Navigate to `/admin/projects/new` (or edit an existing project). Verify the project form renders with project-specific fields (status dropdown, tech stack input, screenshot upload, repo URL, demo URL). Confirm the BlockNote editor is NOT present.
**Expected:** Project form with full project-specific UI. No text editor, no slash menu, no BlockNote block controls. The `kind === 'project'` conditional correctly renders `ProjectForm` component.
**Why human:** Conditional component rendering (`kind === 'project'` vs article/note) needs browser verification to confirm correct branch.

### Gaps Summary

No code-level gaps found. All 13 must-have truths from the three plan frontmatters are VERIFIED through codebase evidence. All 8 artifacts exist, are substantive (not stubs), and are wired to their data sources. All 4 key links are connected. No anti-pattern blockers detected.

The status is `human_needed` because 8 browser-level verification items remain -- these cover visual appearance, interaction behavior, and API roundtrip fidelity that cannot be verified through static code analysis alone.

**REQUIREMENTS.md discrepancy:** HOME-02 is mapped to Phase 5 (Pending) in the REQUIREMENTS.md traceability table (line 165), but evidence shows it was implemented in Phase 02.1. The table should read `HOME-02 | Phase 02.1 | Complete`.

---

_Verified: 2026-05-23T00:00:00Z_
_Verifier: Claude (gsd-verifier)_
