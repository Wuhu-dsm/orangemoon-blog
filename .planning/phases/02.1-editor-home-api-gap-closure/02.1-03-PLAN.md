---
phase: 02.1-editor-home-api-gap-closure
plan: "03"
type: execute
wave: 2
depends_on:
  - "02.1-01"
files_modified:
  - frontend/src/components/home/FeaturedProjects.tsx
  - frontend/src/components/home/ProjectSubscribeSection.tsx
autonomous: true
gap_closure: true
requirements:
  - HOME-03
  - HOME-09
must_haves:
  truths:
    - "FeaturedProjects must not silently disappear when the public projects API returns an empty list"
    - "Empty projects are the current bug state; hiding the whole module is not an acceptable target behavior"
    - "FeaturedProjects must preserve the home layout in loading, empty, and error states"
    - "FeaturedProjects must keep using real project data from GET /api/v1/projects when published projects exist"
  artifacts:
    - path: "frontend/src/components/home/FeaturedProjects.tsx"
      provides: "Featured project section with real data and stable empty/error fallbacks"
      contains: "usePublicProjects"
    - path: "frontend/src/components/home/ProjectSubscribeSection.tsx"
      provides: "Home composition wrapper that should not collapse unexpectedly"
      contains: "FeaturedProjects"
  key_links:
    - from: "frontend/src/components/home/FeaturedProjects.tsx"
      to: "frontend/src/hooks/usePublicContent.ts"
      via: "usePublicProjects hook"
      pattern: "usePublicProjects"
---

<objective>
Fix the current homepage project-section bug: when the public projects API returns no items, the module currently returns `null` and silently disappears. The target behavior is a stable, design-matching empty or fallback state, not hiding the module.

Output: FeaturedProjects continues to show real projects when available and shows a compact, intentional empty/error fallback when data is empty or unavailable.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-content-management-core/02-10-SUMMARY.md

<current_findings>
- `FeaturedProjects.tsx` already calls `usePublicProjects({ pageSize: 10 })` and maps API data into display cards.
- Current empty-state code is:
  `if (visibleProjects.length === 0) { return null }`
- That behavior is the bug reported by the user: the module disappears when the backend returns an empty array.
- `ProjectSubscribeSection.tsx` only wraps `<FeaturedProjects />` in a flex column. If FeaturedProjects returns null, the entire project module area can vanish without any user-facing state.
</current_findings>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace null empty state with a design-matching project empty state</name>
  <files>frontend/src/components/home/FeaturedProjects.tsx</files>
  <action>
1. Remove the `return null` empty state.
2. Keep the same section shell and header used for loaded projects.
3. Render an empty state inside the section:
   - preserve `ShowcaseGrid` height or an equivalent fixed-height content area so the home layout does not jump
   - use a subtle empty card or compact message such as "项目正在整理中"
   - keep the "查看全部" link if the future `/projects` page is still the intended destination; otherwise make it visually disabled only if the surrounding app already has that pattern
4. Do not use mock project cards as if they were real data.
5. Avoid oversized empty-state illustration or marketing copy; this is a compact dashboard section.
  </action>
  <verify>
    <automated>cd frontend && npm run lint && npm run build</automated>
    <manual>Mock `usePublicProjects` or backend response to `{ items: [], total: 0 }`; the FeaturedProjects section remains visible with a stable empty state.</manual>
  </verify>
  <done>
Empty project data no longer causes the module to disappear; the user sees a compact, intentional empty state.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add non-breaking error fallback for project API failures</name>
  <files>frontend/src/components/home/FeaturedProjects.tsx</files>
  <action>
1. Destructure `isError` from `usePublicProjects`.
2. When the query errors, render the same section shell with a short fallback state.
3. Do not redirect to login or throw from the component. The existing `apiClient` handles 401 globally, but this public route should normally be unauthenticated.
4. Keep project card rendering unchanged when data exists.
5. If the component needs helper functions for skeleton/empty/error cards, extract small local subcomponents in the same file instead of duplicating large JSX blocks.
  </action>
  <verify>
    <automated>cd frontend && npm run lint && npm run build</automated>
    <manual>Simulate network/API failure. The home page remains usable and the project area shows a compact fallback instead of crashing or vanishing.</manual>
  </verify>
  <done>
Project API failures are visible but non-blocking, and no stale mock project data is shown.
  </done>
</task>

<task type="auto">
  <name>Task 3: Preserve card layout quality for real project data</name>
  <files>frontend/src/components/home/FeaturedProjects.tsx</files>
  <action>
1. Keep `visibleProjects = displayProjects.slice(0, 3)` for real data.
2. Preserve the current `ShowcaseGrid` card sizing and active-card overlay.
3. Ensure long project names, long summaries, and missing cover images do not overflow or resize the section.
4. Keep existing fallback gradient for missing `coverImage`.
5. Verify Chinese status labels for `pending`, `developing`, `updating`, and `archived` still render.
6. Consider extracting repeated shell/header markup into a small local `FeaturedProjectsShell` if empty/error/loading states would otherwise duplicate the same wrapper.
  </action>
  <verify>
    <automated>cd frontend && npm run lint && npm run build</automated>
    <manual>Use real or mocked projects with long titles/summaries and missing cover images. Cards stay within the fixed section height without overlap.</manual>
  </verify>
  <done>
Real project data, missing covers, long text, loading, empty, and error states all preserve the homepage section dimensions and visual polish.
  </done>
</task>

</tasks>

<threat_model>
## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02.1-03-01 | Information Disclosure | Public projects API | accept | Backend `findAllPublic` already filters to published and non-deleted projects. |
| T-02.1-03-02 | Spoofing / Misleading Content | Empty project state | mitigate | Empty state must not render fake project cards or old mock data as real content. |
</threat_model>

<verification>
Final verification for this plan:
1. `cd frontend && npm run lint`
2. `cd frontend && npm run build`
3. Home page with published projects: real cards render.
4. Home page with empty project list: section remains visible with stable empty state.
5. Home page with project API failure: section remains visible with non-breaking fallback.
</verification>

<success_criteria>
- [ ] `FeaturedProjects` no longer returns `null` for empty API data.
- [ ] Empty state is visible, compact, and design-aligned.
- [ ] Error state is visible enough to explain absence without breaking the page.
- [ ] Real project cards still render from `usePublicProjects`.
- [ ] No mock project data is shown as real content.
- [ ] Layout does not jump, overflow, or overlap across desktop and mobile widths.
</success_criteria>

<output>
Create `.planning/phases/02.1-editor-home-api-gap-closure/02.1-03-SUMMARY.md` when done.
</output>
