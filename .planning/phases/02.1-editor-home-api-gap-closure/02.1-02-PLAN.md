---
phase: 02.1-editor-home-api-gap-closure
plan: "02"
type: execute
wave: 2
depends_on:
  - "02.1-01"
files_modified:
  - frontend/src/api/publicContent.ts
  - frontend/src/hooks/usePublicContent.ts
  - frontend/src/components/home/LatestArticles.tsx
autonomous: true
gap_closure: true
requirements:
  - HOME-02
  - HOME-09
  - ARTC-07
must_haves:
  truths:
    - "LatestArticles must fetch real published article data from GET /api/v1/articles"
    - "LatestArticles must not keep the current hardcoded mock article array as the normal data source"
    - "LatestArticles must handle loading, empty, and error states without breaking the home layout"
    - "Read count display must not fake historical analytics; use an optional API field when present, otherwise use an honest zero/placeholder until DATA-01 adds analytics"
  artifacts:
    - path: "frontend/src/api/publicContent.ts"
      provides: "Public article API client next to the existing public project API client"
      contains: "fetchPublicArticles"
    - path: "frontend/src/hooks/usePublicContent.ts"
      provides: "React Query hook and query key for public articles"
      contains: "usePublicArticles"
    - path: "frontend/src/components/home/LatestArticles.tsx"
      provides: "Home latest article section backed by live API data"
      contains: "usePublicArticles"
  key_links:
    - from: "frontend/src/components/home/LatestArticles.tsx"
      to: "GET /api/v1/articles"
      via: "usePublicArticles -> fetchPublicArticles -> apiClient"
      pattern: "fetchPublicArticles"
---

<objective>
Replace the homepage latest-articles mock data with the real public article API while preserving the current visual layout and making empty/loading/error states explicit and stable.

Output: `LatestArticles` renders real published articles from `GET /api/v1/articles`, with no static article array as the normal path.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/CONVENTIONS.md

<current_findings>
- `frontend/src/components/home/LatestArticles.tsx` currently defines a local static `articles` array and renders `visibleArticles = articles.slice(0, 3)`.
- Backend already exposes `GET /api/v1/articles` through `ArticleController.findAll`, and `ArticleService.findAllPublic` filters to `status: Published` and `deletedAt: null`.
- `frontend/src/api/publicContent.ts` currently only contains `PublicProject` and `fetchPublicProjects`.
- `frontend/src/hooks/usePublicContent.ts` currently only contains project query keys and `usePublicProjects`.
- `ArticleSchema` does not currently include read-count fields; DATA-01 owns real analytics later.
</current_findings>

<interfaces>
Existing public response envelope:

```typescript
export interface ApiEnvelope<T> {
  code: number
  data: T
  message: string
}

export interface PublicListResponse<T> {
  items: T[]
  total: number
}
```

Article fields currently exposed by backend documents:

```typescript
interface PublicArticle {
  _id: string
  title: string
  slug: string
  summary?: string
  coverImage?: string
  tags: string[]
  category?: string
  body?: { blocks: BlockContent }
  publishedAt?: string
  createdAt: string
  updatedAt: string
  viewCount?: number
  readCount?: number
  views?: number
}
```

The optional view fields are defensive frontend compatibility only; do not add fake backend analytics in this phase.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add public article API client and React Query hook</name>
  <files>
    frontend/src/api/publicContent.ts
    frontend/src/hooks/usePublicContent.ts
  </files>
  <action>
1. Extend `publicContent.ts` with `PublicArticle` and `fetchPublicArticles`.
2. Reuse the existing `PublicListResponse<T>` type; do not duplicate list response types.
3. `fetchPublicArticles` should call `apiClient.get<unknown, ApiEnvelope<PublicListResponse<PublicArticle>>>('/articles', { params })` and return `response.data`.
4. Params should support at least `page`, `pageSize`, `tag`, `category`, and `search`, matching `ArticleQueryDto`.
5. Extend `publicContentKeys` with `articles.all` and `articles.list(params)`.
6. Add `usePublicArticles(params?)` with `staleTime: 5 * 60 * 1000`, matching public project caching.
7. Export `PublicArticle` from the hook module if the component needs the type.
  </action>
  <verify>
    <automated>cd frontend && npm run lint && npm run build</automated>
  </verify>
  <done>
`usePublicArticles({ pageSize: 3 })` compiles and calls `GET /api/v1/articles` through the shared public content API layer.
  </done>
</task>

<task type="auto">
  <name>Task 2: Refactor LatestArticles to live API data</name>
  <files>frontend/src/components/home/LatestArticles.tsx</files>
  <action>
1. Remove the local `Article` type and hardcoded `articles` array.
2. Import `usePublicArticles` and map `data?.items ?? []` to the existing display shape.
3. Preserve the existing section shell, `ShowcaseGrid`, card sizing, hover behavior, active overlay, and "查看全部" link.
4. Map fields:
   - id: `_id`
   - title: `title`
   - excerpt: `summary ?? ''`
   - category: `category ?? first tag ?? '文章'`
   - date: `publishedAt ?? updatedAt ?? createdAt`, formatted as `YYYY/MM/DD`
   - cover: `coverImage` when present, otherwise use a non-broken fallback visual inside the card
   - views: optional `viewCount ?? readCount ?? views ?? 0`; do not preserve old mock values like `1.2k`
5. Keep category badge colors deterministic. If category is not one of the old static values, derive a stable class from index or default to the current sky/blue style.
6. Avoid text overflow in compact cards: keep `line-clamp`, `truncate`, and stable card heights.
  </action>
  <verify>
    <automated>cd frontend && npm run lint && npm run build</automated>
    <manual>With published articles present, home page shows their real titles/summaries/covers/dates from the API. No old mock titles remain in normal rendering.</manual>
  </verify>
  <done>
LatestArticles displays up to three real published articles from the public API and no longer depends on static mock data.
  </done>
</task>

<task type="auto">
  <name>Task 3: Add stable latest-article loading, empty, and error states</name>
  <files>frontend/src/components/home/LatestArticles.tsx</files>
  <action>
1. While loading, render the same section shell with three skeleton cards that match the existing card dimensions.
2. When the API returns zero published articles, render a compact empty state inside the section shell rather than collapsing the home layout.
3. When the query errors, render a small non-blocking fallback state inside the section shell; the home page must not crash or redirect.
4. Keep visible empty/error copy short and user-facing, not implementation-explanatory.
5. Do not introduce retry loops or new dependencies.
  </action>
  <verify>
    <automated>cd frontend && npm run lint && npm run build</automated>
    <manual>Simulate empty API and failed API states. The LatestArticles section remains visually stable and does not show stale mock data.</manual>
  </verify>
  <done>
LatestArticles has explicit loading, empty, and error states that preserve home layout quality.
  </done>
</task>

</tasks>

<threat_model>
## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02.1-02-01 | Information Disclosure | Public articles API | accept | Backend public service already filters to published, non-deleted articles; this plan only consumes that route. |
| T-02.1-02-02 | Spoofing / Misleading Metrics | Read count display | mitigate | Do not carry over mock view counts. Show optional API metrics only when present; otherwise show 0/placeholder until analytics phase. |
</threat_model>

<verification>
Final verification for this plan:
1. `cd frontend && npm run lint`
2. `cd frontend && npm run build`
3. Home page with published articles: real article cards render.
4. Home page with zero articles: section shows stable empty state.
5. Home page with article API failure: section shows non-breaking fallback and no stale mock data.
</verification>

<success_criteria>
- [ ] `publicContent.ts` exports `PublicArticle` and `fetchPublicArticles`.
- [ ] `usePublicContent.ts` exports article query keys and `usePublicArticles`.
- [ ] `LatestArticles` calls the real public API.
- [ ] Old hardcoded article data is removed from normal rendering.
- [ ] Loading, empty, and error states are explicit and layout-stable.
- [ ] No fake historical read counts remain.
</success_criteria>

<output>
Create `.planning/phases/02.1-editor-home-api-gap-closure/02.1-02-SUMMARY.md` when done.
</output>
